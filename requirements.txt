# paint-me-like-your-french-substances skill — ingest.py / select_frames.py dependencies
yt-dlp
openai-whisper    # pip install openai-whisper  (requires torch — already present if ComfyUI is installed)

# System dependencies (not pip):
# ffmpeg — https://ffmpeg.org/download.html
# Windows: winget install ffmpeg   OR   scoop install ffmpeg   OR   choco install ffmpeg
# Must be on PATH for select_frames.py's frame capture (Step 2) to work.
# ingest.py (Step 1) doesn't call ffmpeg, but checks for it on PATH — if missing,
# it marks the tutorial frame_status: skipped (permanently no frames) instead of
# pending-selection.

# deno — https://docs.deno.com/runtime/manual/getting_started/installation
# Windows: winget install deno
# Required by yt-dlp to solve YouTube's n-challenge (format/download throttle bypass).
# Without deno: yt-dlp may fail or return incomplete video format data.

# cookies.txt — NOT a system install, exported manually from browser
# ingest.py auto-tries --extractor-args youtube:player_client=android first
# (no setup needed) to dodge "Sign in to confirm you're not a bot" errors.
# Only export cookies.txt if a video still fails under the android client.
# See SETUP.md → "YouTube Bot Detection" section for export instructions.
# Place as cookies.txt in this skill directory — never commit to git.

# ── Optional: Substance Painter MCP connection (live Painter control, not
#    required for consultant mode / ingest pipeline) ───────────────────────
# mcp                        # pip install mcp — needed by elliezu/SubstancePainterMCP
#                             # (free, MIT, 79 tools) if you set up a live MCP bridge.
# fastmcp                    # pip install fastmcp — needed by diffdaff/substance-painter-mcp
#                             # (free, MIT, 10 tools) instead, if you pick that option.
# Neither is required for ingesting tutorials or consulting this skill — see SETUP.md
# → "Substance Painter MCP Connection (optional)" for full setup steps and the
# third (paid) option, MCP Pro for Painter.
